# State

*(Rewritten in full on every `End Today`. This is what gets read cold at the start of the next session — keep it dense.)*

## Last Session

- **Date:** 2026-07-14
- **Type:** Finished Graphs (3/3) and 1-D DP (3/3); opened Greedy/Intervals (2/4 solved).
- **Problems solved:** **Number of Islands** (medium, DFS flood-fill, O(M×N)/O(M×N)). **Clone Graph** (medium, DFS + hashmap old→new, O(N+E)/O(N)). **Course Schedule** (medium, DFS with 3-state cycle detection, O(N+E)/O(N+E)). **Climbing Stairs** (easy, bottom-up two rolling variables, O(n)/O(1)). **House Robber** (medium, same rolling-variable shape, O(n)/O(1)). **House Robber II** (medium, circular reduction to two House Robber I sub-problems — real bug, see below). **Merge Intervals** (medium, sort + single-pass merge, O(n log n)/O(n)). **Insert Interval** (medium, three-phase single pass reusing the already-sorted input, O(n)/O(n)).
- **What got built:** 8 new Phase 1 modules in `index.html`'s `PHASE1_PROBLEMS`, plus real new shared infra: `renderP1Grid` (Graphs' first 2D-grid visual), `renderP1GraphSVG` (circular node/edge layout, later extended with directed arrowheads + 3-color DFS states + zero-indexed labels + custom highlight color — reused for Clone Graph, Course Schedule, and House Robber II's circular-adjacency visual), `renderP1ChosenRow` (House Robber's valid/invalid selection row), `renderP1Timeline` (Greedy/Intervals' first visual — auto-stacking interval bars, built for Merge Intervals, reused as-is for Insert Interval). Climbing Stairs needed zero new infra — its decision-tree visual reused Trees' `renderP1TreeFromRoot` unmodified. All verified live in headless Chrome (`puppeteer-core` + `python3 -m http.server 5501`; the cached Chrome at `mac_arm-150.0.7871.24` is corrupted, `mac_arm-136.0.7103.94` works), full regression after every addition.
- **Weak area flagged:** House Robber II's first submission was the House Robber I linear algorithm pasted in unchanged, with no adaptation for the circular constraint — failed 3 of 7 test cases. Not the (resolved) `this.`-mechanics slip; this is pattern-matching to "looks like the last problem" without checking the new twist got handled. Flagged the exact failing cases; Lawrence asked for the fix directly, which I did. One occurrence so far — not yet in `notes.md`, watch for recurrence on other "variant of a previous problem" pairs.
- **Win:** Two more full patterns closed (Graphs, 1-D DP) — 9 patterns fully complete now. Caught and fixed 3 separate bulk-import defects before Lawrence saw them (Clone Graph's missing `Node` class, Climbing Stairs' wrong complexity claim, House Robber's wrong `// Expected` comment), plus proactively strengthened Clone Graph's weak test assertions. 45 of 47 total reference problems now solved.

## Next Session Starting Point

Continue **Greedy / Intervals** (2/4 solved: Merge Intervals, Insert Interval done). Next up: **Non-overlapping Intervals** — its baked file (`nonOverlappingIntervals.js`) was already verified this session (all explicit test cases *and* its existing 100-case silent-pass battery all pass, so it and its oracles are trustworthy) but **not yet stubbed** — start there. Build its Phase 1 module reusing `renderP1Timeline` (no new infra needed). Then **Meeting Rooms II** (`intervals-meetingRoomsII.js`, heap-based, the hardest of the four) to close Greedy/Intervals at 4/4. Standard flow: verify baked file → stub → build Phase 1 module → verify live with full regression → assign. After Greedy/Intervals, check `ls` again — Tries, Heap/Priority Queue, Backtracking, Advanced Graphs, 2-D DP, Bit Manipulation, and Math & Geometry all currently have 0 imported files, so nothing to solve there until new files are added.
