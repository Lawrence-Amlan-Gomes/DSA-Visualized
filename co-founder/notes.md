# Notes

*(Freeform, persistent observations for future-me — recurring patterns, teaching adjustments, things worth remembering across many sessions that don't belong in state.md's single-session snapshot or roadmap.md's checklist. Append or edit in place; no fixed structure.)*

- **"Solved" means worked through live, not "a file exists that runs."** Learned this the hard way on 2026-07-02: the roadmap said 9 patterns were mastered based purely on solution files existing in the repo, but Lawrence had never actually gone through them with the mentor — they were imported/inherited, not personally derived. Roadmap was reset to 0/17. Going forward, only mark a pattern `[x]` after a real session where Phase 2's discipline was followed live (brute force stated, optimized, complexity re-derived, edge cases covered, run in LiveCoding). Never infer mastery from git history or file presence again.
- **Verify understanding, not recall, once real practice starts.** This is still the right instinct for DP recurrences and anything else that's easy to pattern-match from having seen a solution before — probe with "why," not just "what."
- **Write everything — UI copy and live chat explanations — in plain, simple English.** Learned 2026-07-02 while building Phase 0's interactive module: the first draft of Section 5 used dense phrasing ("auxiliary structure," "space complexity is whichever one dominates," "pushed/popped onto the stack") and Lawrence explicitly asked for it to be rewritten in easier English. Short sentences, everyday words, numbered points over dense multi-clause sentences. Default to this the first time, don't wait to be asked.
- **Interactive teaching sections need a way forward at the bottom, not just the top.** Learned 2026-07-02: Phase 0's section nav pills live at the top of the page; the first version of each section ended with plain text like "Next: Section 2" with nothing to click, so Lawrence finished reading, hit the bottom, and had no way to continue without scrolling back up. Fixed by giving every section a real "Continue to Section N →" button at the bottom that switches sections and scrolls to top. Apply this to any future interactive/multi-section teaching UI, not just Phase 0.
- **Don't linger in Q&A once Lawrence answers correctly.** Said directly 2026-07-02 during the Contains Duplicate check-in: "you will not waste time chatting with me here, make the next step." The Phase 2 discipline (brute force → optimize → complexity → edge cases) still gets verified with questions every problem — but the moment his answers are correct, confirm briefly and move straight to assigning/building the next step. No extra rounds of follow-up once he's right.
- **Do not ask gut-check/quiz questions at all once code is verified correct — go straight to building the next problem.** Sharpened 2026-07-06 after Product of Array Except Self: he fixed a real bug (method didn't return), I re-ran it, all outputs matched — then I asked an unprompted "why does `-0` show up here?" curiosity question instead of moving on, and he pushed back explicitly: "What is our rule? do not ask me any questions. if my code is correct build the next one." This goes further than the 2026-07-02 note above — that one was about not lingering after HE answers a question; this one means don't originate new questions either. If code is correct (ran, output matches expected comments), confirm in one line and immediately stub + build the next problem's Phase 1 module. Save any interesting side-observations (like `-0`) for a passing comment, not a question.
- **He now keeps his brute-force attempt as commented-out code above the optimal solution, instead of deleting it.** Adopted 2026-07-02, after his Contains Duplicate brute force got deleted before the file was reviewed, which triggered an unnecessary "did you skip brute force?" callout — he had done it, just hadn't kept it. Check the file for a commented-out brute-force block before assuming the step was skipped; don't make him re-explain it verbally if it's already there.
- **Bulk-imported solution files often already contain the full answer.** All 47 reference files in LiveCoding were imported from an old project, and several (containsDuplicate, twoSum, validAnagram so far) had complete working solutions already written in, which would spoil the reveal-gated Phase 1 workflow if left as-is. Stub any file (problem comment + empty method + test calls only) before pointing Lawrence at it for a real session — assume it's baked until checked. Documented in `CLAUDE.md` too since it's an infra-level gotcha, not just a teaching note.
- **The bulk-imported files' own `// Expected:` comments aren't reliable — run the baked solution before trusting its test oracle.** Caught twice on 2026-07-06, same session: Longest Consecutive Sequence's test case `[9,1,4,7,3,-1,0,5,8,-1,6]` was commented `Expected: 9` but the actual correct answer is 7 (gap at 2 splits the run — sorted unique values are -1,0,1,3,4,5,6,7,8,9, so the longest run is 3-4-5-6-7-8-9, not the whole span). First Unique Character's last two test cases named the wrong character in the comment even where the numeric index happened to be correct. Both were bugs in the inherited annotation, not in the underlying algorithm or in Lawrence's later fix — but if left uncorrected they'd have made a genuinely correct solution look wrong. Before stubbing any remaining bulk-imported file, run the existing baked solution first and manually verify each `// Expected:` line, not just check whether a `class Solution` already exists.
- **Some "brute force" isn't just slower — it's outright wrong, and that's worth calling out explicitly.** Encode and Decode Strings' naive approach (join with a delimiter, split to decode) isn't a valid-but-slow baseline like most brute forces — it silently produces the wrong answer whenever a string contains the delimiter. Don't force every problem into the standard "brute force O(x²) → optimal O(x)" framing when the naive approach is actually incorrect; name that distinction directly when it comes up (it did again conceptually with Reverse Words, where both approaches are O(n) and the real difference is robustness/readability, not Big-O — also worth naming explicitly rather than pretending there's an asymptotic win).
- **Open tension, not yet resolved: submitted code sometimes matches the reveal panel word-for-word.** Noticed 2026-07-07 on both Valid Palindrome and 3Sum — the code *and* the complexity comments Lawrence submitted were identical to the reveal panel's text, not paraphrased (down to matching variable names and the exact wording of the Time/Space blurb). Raised it as a direct question after the second occurrence; he shut it down immediately, invoking the standing "don't question correct code, build the next one" rule from the entry below, and I deferred per the pushback edge case in `skills/skillCoFounderMentor.md`. This is genuinely different in kind from a knowledge quiz — it's about whether the reveal-gating (the entire point of Phase 1's design) is doing anything — but he's made clear it's not to be raised as a question again. If it keeps happening, the fix has to come through a different channel: watching him solve live, or noticing if a *wrong* attempt ever gets submitted (proof he's actually struggling with it first), not interrogating the final file. Don't re-raise this as a gut-check; that channel is closed.
- **Don't ask questions once code is correct — this now extends beyond quiz/gut-checks to any originated question at all, even process concerns.** Sharpened 2026-07-07: the 2026-07-06 rule (below) was about not quizzing on algorithm understanding. Lawrence's 2026-07-07 pushback ("what is our rule, if my code is right, build the next one") makes clear the rule covers *any* question I originate after correct code, including ones about his process (e.g. "did you copy this from the reveal?"). If code runs and matches expected output, the only acceptable response is a one-line confirmation followed immediately by the next problem — full stop, no exceptions carved out for legitimate-seeming concerns.
- **Recurring JS-mechanics slip: confusing which identifiers need `this.` and which don't.** Started 2026-07-10 as "forgot `this.` when calling a class method": Merge K Sorted Lists' `mergeKLists` called its own `mergeTwoLists` helper bare instead of `this.mergeTwoLists(l1, l2)`; Invert Binary Tree's `invertTree` called itself recursively the same bare way. Recurred 2026-07-11 on Maximum Depth of Binary Tree (`maxDepth` called itself bare) and Same Binary Tree (`isSameTree` called itself bare) — for both, per the escalation below, pointed at the line/error and had Lawrence fix it himself rather than patching it inline. Then Subtree of Another Tree (also 2026-07-11) showed the *mirror* mistake in the same file: `this.sameTree(...)` where `sameTree` was a plain nested function already in scope by its bare name (didn't need `this.` at all), sitting right next to a bare `isSubtree(...)` call that *did* need `this.` since it's the real class method. So the actual gap isn't "always forgets `this.`" — it's not yet automatic which identifiers are class methods (need `this.`) versus plain local functions/params (don't). Five occurrences across two sessions now. Still a mechanics gap, not an algorithm misunderstanding — keep pointing at the exact line/error rather than quizzing, but have him fix it himself (stopped fixing it for him after the third occurrence) rather than patching silently. If a sixth occurrence shows up, worth naming the pattern explicitly rather than just fixing the line.
  - **Update 2026-07-13: appears resolved.** Six submissions in a row this session (Level Order, Valid BST, Kth Smallest, Construct Tree, Max Path Sum, Serialize/Deserialize) all correctly distinguished bare nested-function calls (`dfs`, `build`, `valid`, `visit`) from real class-method calls needing `this.`, with zero slips — including files with multiple nested helper functions in play at once, exactly the shape that tripped him up before (e.g. Subtree of Another Tree's dual-direction mistake). Treat as closed going forward; only reopen this note if it resurfaces.
